# Measured economy implementation

User authorized populating the metrics properly, retaining the prior requirement of no synthetic runtime data. Use the supplied final_paper_clean_figures_visual.pdf as the formula specification and retain verifiable live inputs. Correct the previous conflation of revenue R and independently verified output value V with an explicitly versioned v2 methodology.

1. Collect finalized, block-pinned USDC balances for distinct active executor wallets, current active-agent state, original settlement/delivery/ack timestamps, and registered service prices. A failed balance read must mark capital incomplete; never substitute zero. Expose today's UTC activity and lifetime activity, refund totals, gross allocations, agent completion activity and spend/balance turnover. Name wallet balance and operational activity precisely; these are not total economic capital or independent productivity.
2. Build an explicit equal-weight five-category registered-quote basket from the earliest service in each category. Pin its baseline at the first complete basket. Compare only the same seller/category/unit/quantity/endpoint family. Persist reproducible provenance in snapshots; compute previous closed-day values only when the whole basket actually existed. Show the initial baseline and pending first day honestly, not invented historical inflation. Display purchasing power with units and hashes.
3. Make live measurements the default view, with UTC date range and finalized-block time. Apply paper equations 8–9 as GAP = revenue minus intermediate inputs, equation 2 as surplus = revenue minus total resource cost, and equation 3 as productivity = independently verified output value divided by total cost. Revenue is the finalized seller allocation before separately measured refunds. Require complete inputs for each metric independently. Preserve historical v1 records without comparing them to v2 indexes. Expose exact evidence coverage and a seller-signed production accounting workflow. Bind claims to finalized acknowledged outputs, authenticate ownership, enforce input allocation conservation and event ordering, and retain immutable provenance. Producer cost reports cannot create independently verified output values. The paper supplies definitions, not actual cost records, capital measurements, or valuations.
4. Add meaningful metric and indexer regressions, run relevant real database/browser checks, full tests/typecheck/build, deploy, refresh real indexing and inspect populated production values. Publish evidence and commit/push. Preserve all private operation journals and preexisting untracked assets.
