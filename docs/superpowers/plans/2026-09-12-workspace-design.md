# Workspace clarity and end-to-end verification

User authorizes improving the existing workspace layout with flat 2D icons/illustrations and no shadows, plus end-to-end and edge-case checks. Retain real runtime execution; Ledger hardware alone is emulated. Base dfb8068.

1. Root: inspect incumbent/live truth; improve Economy hierarchy and changing-value clarity, resource/agent visuals and shared workspace polish within the existing monochrome/coral identity. Add soft mint/blue surface accents and crisp vector geometry. Preserve exact values, missing-evidence semantics and accessible controls. Do not invent historical charts or output valuations.
2. Independent implementation: fix Evidence hiding economy jobs; use the existing owner-scoped activity API and preserve research history with accurate labels. Add isolated browser regressions for resource receipts and owner/agent switching.
3. Root: repair refresh ordering/staleness if reproduced, test midnight/empty/stale/failure/retry cases, actual provider health, finalized receipts and storage expiry. Reuse original completed intents for replay checks; do not issue unnecessary new payments.
4. Review final changes independently; test application/DB, browser, typecheck and build; inspect desktop/mobile in bounded passes. Deploy, verify authenticated live pages and API boundaries, capture evidence, scan secrets, commit and push. Preserve private journals and unrelated untracked assets.
