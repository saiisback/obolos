import { WorkspaceDevelopers } from '@/components/platform/workspace-developers';

export const metadata = { title: 'API credentials | Obolos workspace' };

export default function WorkspaceDevelopersPage() {
  return <WorkspaceDevelopers />;
}
