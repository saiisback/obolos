import {EconomyWorkspace} from '@/components/platform/economy-workspace';

export const metadata = {title: 'Economy | Obolos workspace'};

export default function EconomyPage() {
  return <EconomyWorkspace />;
}
